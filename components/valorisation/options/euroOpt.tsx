import React from "react";

type EuroOptionProps = {
  countries: any;
  currencies: any;
};

const EuroOption = ({ countries, currencies }: EuroOptionProps) => {
  return <div>EuroOption</div>;
};

export default EuroOption;
