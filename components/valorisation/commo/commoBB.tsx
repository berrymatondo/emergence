import React from "react";

type CommoBackBondProps = {
  countries: any;
  currencies: any;
};

const CommoBackBond = ({ countries, currencies }: CommoBackBondProps) => {
  return <div>CommoBackBond</div>;
};

export default CommoBackBond;
