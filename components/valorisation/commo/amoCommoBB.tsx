import React from "react";

type AmoCommoBackBondProps = {
  countries: any;
  currencies: any;
  schedules: any;
};

const AmoCommoBackBond = ({
  countries,
  currencies,
  schedules,
}: AmoCommoBackBondProps) => {
  return <div>AmoCommoBackBond</div>;
};

export default AmoCommoBackBond;
